[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "python -m spacy download en_core_web_lg"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT --timeout 300 --workers 2 src.app:app"
